return {
    name = "dotenv",
    version = "1.0.0",
    description = "Parse .env files and INI-style key/value documents; optionally merge into the process environment via @sys",
    license = "MIT",
    dependencies = {
        filesystem = "1.0.0",
        sys = "1.0.0",
    },
}
